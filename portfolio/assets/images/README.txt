Place Carlo's personal photo here as:

    carlo.png

Requirements:
- PNG format with transparent background (recommended)
- Portrait orientation, ~800 x 900 px
- The photo will appear on the right side of every portfolio page

Once the file is placed here, the right-side image on all pages will display automatically.
